<?php
/**
 * Template Name: SmartJob App
 * Description: Full-width template for embedding the SmartJob application
 */

// Remove WordPress header
remove_action('wp_head', '_wp_render_title_tag', 1);
remove_action('wp_head', 'wp_enqueue_scripts', 1);
remove_action('wp_head', 'wp_resource_hints', 2);
?>
<!DOCTYPE html>
<html <?php language_attributes(); ?>>
<head>
    <meta charset="<?php bloginfo('charset'); ?>">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SmartJob - Job Search Platform</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
        }
        .app-container {
            width: 100%;
            height: 100vh;
            overflow: hidden;
        }
        .app-frame {
            width: 100%;
            height: 100%;
            border: none;
        }
    </style>
    <?php wp_head(); ?>
</head>
<body>
    <div class="app-container">
        <iframe 
            src="https://smartjobconnekt.netlify.app" 
            class="app-frame"
            title="SmartJob Application"
            allowfullscreen>
        </iframe>
    </div>
    <?php wp_footer(); ?>
    
    <script>
    // Improve communication between iframe and parent
    window.addEventListener('message', function(event) {
        // Only accept messages from your Netlify app
        if (event.origin !== 'https://smartjobconnekt.netlify.app') return;
        
        // Handle messages, such as changing page title
        if (event.data.type === 'setTitle') {
            document.title = event.data.title + ' - SmartJob';
        }
        
        // You can add other message handlers here
    });
    </script>
</body>
</html>